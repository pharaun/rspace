use crate::asm::parser::AsmInst;

// Extract the label location, label usage, etc
// - Next step will be to iterate over address generation
pub(super) fn extract(_input: &Vec<AsmInst>) -> () {
    todo!()
}
